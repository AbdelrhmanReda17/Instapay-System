package Controllers;

import User.Account;

public class TransferController {
    public boolean transfer(Account distAccount, Account srcAccount, double amount) {
        return true;
    }
    public boolean isValidTransaction(Account distAccount, Account srcAccount) {
        return true;
    }
}
